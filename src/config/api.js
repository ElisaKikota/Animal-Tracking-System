// API Configuration
const API_CONFIG = {
    BASE_URL: 'https://animal-tracking-system-backend.onrender.com',
    ENDPOINTS: {
        TRAIN: '/train',
        PREDICT: '/predict',
        HEALTH: '/health'
    }
};

export default API_CONFIG; 